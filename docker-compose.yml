version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: letswatch-db
    environment:
      MYSQL_DATABASE: letswatchdb
      MYSQL_ROOT_PASSWORD: password
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"